# 班級像素小鎮 — 實作計畫書

給 Claude Code Agent 的規格。請完整讀完再動工，特別是「不要做的事」與「安全設計」兩節。

---

## 0. 這是什麼

一位老師要在課堂投影一個像素風網頁：每個學生對應一隻貓，站在一張俯視地圖上，頭上有名字和血條。血條代表學期分數。學生上課表現好，老師點那隻貓，打開角色面板，按一下加分，貓當場升級，全班看得到。

用途是課堂激勵，不是正式成績系統，也不是遊戲。**唯一的使用者是老師**，在自己的電腦上執行、投影給全班看。學生不登入、不操作、不連線。

### 成功長什麼樣子

老師雙擊 `啟動.command`（或 `啟動.bat`），瀏覽器自動開啟。第一次會問要用哪個資料夾，選 `data/`，之後這個授權在同一台電腦上會記住。畫面出現草地地圖與三十幾隻貓。學生答對問題，老師點那隻貓，右側滑出角色卡，按「+1」，貓的血條長一截，若跨過等級門檻就播一段小小的升級動畫。分數立刻寫回 `data/class-data.json`。按錯了就按復原。

---

## 1. 技術約束

- **純靜態前端**：HTML + CSS + 原生 JavaScript。無框架、無建置步驟、無 npm、無 bundler。
- **透過 `http://localhost` 執行**，不是 `file://`。這是硬性條件——File System Access API 只在安全來源下可用。
- 除了啟動用的一行伺服器指令外，**沒有後端**。沒有資料庫、沒有網路請求、沒有 API 金鑰。整個專案離線可用。
- 目標瀏覽器：**桌機版 Chrome 或 Edge 最新版**。File System Access API 在 Firefox 與 Safari 不支援，這是已知且接受的限制。偵測到不支援時顯示明確訊息告知請改用 Chrome，不要靜默失敗。
- 地圖與貓用 `<canvas>` 繪製；角色面板、工具列等 UI 用 DOM。名字與血條畫在 canvas 上（跟著貓走，不要用 DOM 疊）。

### 專案結構

```
/
  index.html
  啟動.command            ← macOS / Linux
  啟動.bat                ← Windows
  css/
    main.css
    panel.css
  js/
    config.js             ← 老師唯一需要編輯的檔案
    storage.js            ← 檔案讀寫、授權、備份
    model.js              ← 學生資料、等級計算、異動紀錄、復原
    assets.js             ← 圖檔載入與 spritesheet 切割
    map.js                ← 地圖產生與繪製
    cats.js               ← 貓的狀態、動畫、繪製、命中測試
    panel.js              ← 角色面板 UI
    main.js               ← 進入點、主迴圈
  assets/
    tiles/                ← Cainos 素材
    cats/                 ← Last tick 素材
  data/
    class-data.json       ← 成績與異動紀錄（程式讀寫）
    class-data.example.json
    backups/              ← 自動備份，程式寫入
  tools/
    spritesheet-viewer.html
  CREDITS.md
  README.md               ← 給老師看的中文操作說明
```

---

## 2. 啟動方式

`啟動.command` 與 `啟動.bat` 要做三件事：切到專案目錄、起一個靜態伺服器、開瀏覽器。

依序嘗試 `python3 -m http.server`、`python -m http.server`、`npx --yes serve`，取第一個能用的。三個都沒有時，印出中文說明告訴老師要裝什麼、去哪裡裝，不要只丟一個英文錯誤碼。

固定用 `8173` 這類不常見的埠，避免撞到別的服務。埠被佔用時自動往上找下一個可用的。

`啟動.command` 記得 `chmod +x`，並在 README 說明 macOS 首次執行可能要在「系統設定 → 隱私權與安全性」按允許。

---

## 3. 資料

### 檔案格式

`data/class-data.json`：

```json
{
  "version": 1,
  "updated_at": "2026-09-01T10:23:45+08:00",
  "students": [
    { "id": "s001", "name": "王小明", "hp": 42, "note": "" }
  ],
  "log": [
    {
      "id": "e0001",
      "ts": "2026-09-01T10:23:45+08:00",
      "student_id": "s001",
      "delta": 1,
      "hp_after": 42,
      "reason": "",
      "undone": false
    }
  ]
}
```

老師熟悉 JSON 與 JS，所以格式以結構清晰為優先，不必為了 Excel 相容而降級成 CSV。但仍要防手改出錯：

- 讀取時做 schema 檢查。缺欄位補預設值，型別錯誤就修正並在 console 警告。
- 解析失敗時**不要覆寫檔案**，顯示錯誤橫幅並停在唯讀狀態，讓老師去修檔案。這種時候寫入等於銷毀資料。
- `id` 由程式產生，老師手動新增學生時可以只寫 `name`，載入時自動補 `id`。

`data/class-data.example.json` 放 30 筆假資料，供開發與展示用。README 說明老師要複製一份改名成 `class-data.json` 再填入真實名單。**`class-data.json` 要加進 `.gitignore`**——學生姓名不該進版控。

### 授權與寫入

- 用 File System Access API 的 `showDirectoryPicker()` 取得 `data/` 目錄的讀寫權限。老師選一次目錄，程式就能讀寫其中的 `class-data.json` 與 `backups/`。
- **允許用 IndexedDB 儲存目錄 handle**，讓老師下次開啟時只要按一下確認、不用重新選資料夾。這是 IndexedDB 在本專案唯一的合法用途。
- **成績本身絕對不存在瀏覽器儲存中。** 檔案是唯一真相，記憶體只是當次的快取。任何情況下都不要拿瀏覽器裡的資料去覆蓋檔案裡比較新的資料。
- 每次加分後**去抖動 1 秒再寫檔**（連按多次只寫一次），但頁面關閉前務必 flush。
- 寫入採先寫暫存再取代的方式，避免寫到一半被中斷造成檔案損毀。
- 每天第一次寫入時，把當時的檔案複製一份到 `backups/class-data-YYYY-MM-DD.json`。保留最近 30 份，更舊的自動刪除。

### 重新載入

工具列放一顆「重新載入檔案」。老師若在外部編輯器改了名單，按這顆會重讀。若記憶體中有未寫入的變更，先警告再覆蓋。**不要做檔案輪詢**——網頁自己就是寫入者，輪詢只會製造衝突。

---

## 4. 美術素材

老師會自行下載並放進 `assets/`。不要自己畫圖，也不要用生成式 AI 補素材。素材缺席時用純色方塊當佔位，並在 console 印出清楚的缺檔清單。

### 貓：Last tick — 32x32 Pixel kittens Cats（免費版）

https://last-tick.itch.io/animated-pixel-kittens-cats-32x32

免費版含 3 個 spritesheet（灰、白、薑黃），含全部動畫。允許個人與商業使用，不得單獨轉售。

**spritesheet 的實際排列請自己開檔確認，不要猜。** 先做 `tools/spritesheet-viewer.html`：把 PNG 畫上 canvas、疊 32px 網格線與座標編號、可切換不同圖檔。確認後把動畫定義寫成表：

```js
const CAT_ANIMS = {
  idle:  { row: 0, frames: 4, fps: 6 },
  walk:  { row: 1, frames: 8, fps: 10 },
  sit:   { row: 2, frames: 2, fps: 2 },
  sleep: { row: 3, frames: 4, fps: 3 },
};
```

這個 viewer 要留在 repo 裡，老師之後升級到 43 色完整版時要重新對格。

只有 3 種毛色但有三十幾個學生，靠毛色（3）× 左右翻轉 × 名字標籤區分。名字才是主要辨識。**不要寫程式做色相偏移偽造更多毛色**——原作花紋是手繪的，機器換色會很醜。

### 地圖：Cainos — Pixel Art Top Down - Basic（免費）

https://cainos.itch.io/pixel-art-top-down-basic

32x32，含草地 tileset（256x256）、石地 tileset、牆面 tileset、48 個道具、15 種草、3 棵樹，另有獨立陰影 sprite。下載後請讀壓縮檔內的授權說明並照做。

這套是為 Unity tile palette 設計的，排列同樣要用 viewer 判讀後再寫程式。

### 授權標示

`CREDITS.md` 與畫面頁尾都要標示兩位作者與素材連結。這是使用條件，不是選配。

---

## 5. 地圖

程式產生，不要手排整張圖，不要引入 Tiled 或其 JSON 格式。

- 尺寸由 `MAP_COLS` × `MAP_ROWS` 決定，預設 30 × 20 格。
- 草地為底，用變化磚隨機點綴，加幾條石板路，周圍用樹圍一圈當邊界。
- **必須是決定性的**：用 `MAP_SEED` 餵一個 seeded PRNG（mulberry32 之類），同種子每次結果完全相同。禁止使用 `Math.random()` 產生地圖。
- 道具擺放時記錄佔用格，貓不能生在上面。

### 縮放

- Canvas 內部解析度用地圖原始像素，靠 CSS 放大並設 `image-rendering: pixelated`。
- 縮放倍率取**整數**（2x、3x），不要小數，會糊。
- 視窗放不下就整張縮小置中。不做捲動、不做鏡頭跟隨——全班要一眼看到所有人。

---

## 6. 貓

### 位置與外觀分配

依 `student.id` 做雜湊決定生成位置、毛色、初始朝向。**同一個學生每次開都在同一位置、同一毛色。** 學生會記得自己的貓在哪裡，這點不能隨機。

位置避開道具佔用格，且彼此至少間隔 2 格，避免名字標籤重疊。學生數塞不下時 console 警告並建議加大地圖。

### 動作 = 血量狀態

血條數字是輔助，貓的姿勢才是學生第一眼看到的東西。

| 血量比例 | 狀態 | 動畫 |
|---|---|---|
| 80% 以上 | 精神好 | 站著或緩慢走動，偶爾抬爪 |
| 50–80% | 普通 | 坐著，偶爾動一下 |
| 20–50% | 疲倦 | 趴著 |
| 20% 以下 | 虛弱 | 蜷起來睡 |

實際可用動畫依 spritesheet 而定，上表是意圖。走動用緩慢的隨機漫步，偶爾停下，不要讓貓走得像在趕路，不要走出地圖或走進樹裡。

### 名字與血條

- 名字在貓正上方置中，白字加深色描邊，確保在草地與石地上都清楚。
- 血條在名字下方、貓正上方，寬 32px、高 4px。
- 顏色隨血量：高（綠）→ 中（黃）→ 低（橘）。**不要用紅色**，紅色在教室情境太像懲罰。
- 血條變化要有緩動過渡（約 0.6 秒），不要瞬間跳。
- 字型用系統中文字型堆疊，不載入網路字型。字要夠大，投影機上要看得清。

---

## 7. 互動與角色面板

這是 v2 的核心新增。**不要讓老師直接點貓就加分**——太容易誤觸，而且沒有遊戲感。

### 編輯模式開關

工具列上一個明顯的切換鈕（快捷鍵 `E`）。

- **關閉時（預設）**：點貓沒有任何反應。這是投影展示狀態，防誤觸。
- **開啟時**：貓在滑鼠移上去時有輕微高亮，點擊會開啟角色面板。畫面邊緣加一圈低調的顏色提示目前可編輯。

### 角色面板

點貓後從右側滑入（約 200ms，尊重 `prefers-reduced-motion`）。內容：

- **大張的貓**：把該學生的貓用 4x 放大播放對應血量狀態的動畫。這是面板的視覺主角。
- **姓名**、目前**等級**與稱號。
- **血條**：比地圖上的大得多，顯示 `hp / max_hp`，以及距離下一級還差多少。
- **主要按鈕「+1」**：一顆大按鈕，快捷鍵 `Space` 或 `Enter`。按下後 hp +1，血條動、貓有反應動畫，寫檔。
- **異動紀錄**：這名學生最近 10 筆加分，含時間。
- **復原**：見下節。
- 關閉：`Esc`、點面板外、或右上角關閉鈕。

老師說目前只要 +1。但把加分數值做成 config 陣列 `AWARD_VALUES: [1]`，之後想加 `[1, 3, 5]` 只要改設定就會多出對應按鈕，不用改程式。

### 等級系統

等級由 hp 推導，不另外存。門檻寫在 config：

```js
LEVELS: [
  { at: 0,  title: '見習生' },
  { at: 10, title: '學徒' },
  { at: 25, title: '好奇的貓' },
  // ...
]
```

跨過門檻時播一段**克制**的升級表現：貓身後一圈光暈擴散、面板上的等級數字彈一下、一個短暫的「升級！」字樣。約 1.2 秒後自然消失。

**不要加音效。** 課堂上會很吵，而且一節課可能加分十幾次。

**不要做降級表現。** 若因復原而退回上一級，安靜地改數字就好。

### 復原

老師會按錯，這是必然。

- 工具列常駐一顆「復原」，快捷鍵 `Ctrl/Cmd + Z`。
- 復原堆疊由 `log` 推導，**跨 session 有效**——今天可以復原昨天誤加的分。
- 復原的實作是在 `log` 追加一筆 `delta: -1` 的反向紀錄，並把原紀錄標記 `undone: true`。**不要刪除原紀錄。** 紀錄是老師面對學生質疑時的依據，只能追加不能抹除。
- 復原後跳出一個約 3 秒的提示：「已復原：王小明 +1」，附「取消復原」讓老師改變主意。
- 角色面板內每筆紀錄旁也有各自的復原鈕，方便處理「加錯人」而不是「多加一次」的情況。
- 連按復原可以連續往回退，但一次只退一筆，不要做「一鍵清空今天」這種危險操作。

**除了復原之外，沒有任何減分介面。** 這是刻意的設計決定，見下節。

---

## 8. 安全設計

以下每一條都是為了避免這個工具在教室裡傷到學生。實作時若覺得某條「拿掉也還好」，請先問老師。

- **沒有減分功能**，只有復原誤加。老師目前不打算扣分，介面就不該提供這個選項。
- **血量歸零不做任何死亡表現**：不倒地、不墓碑、不變灰、不消失。最低狀態就是睡覺。這是投影在全班面前的畫面，不能讓任何學生的角色看起來像被公開處刑。
- **血條不用紅色。** 最低用橘色。
- **不做排行榜、名次、或任何把學生互相比較的排序顯示。** 地圖上的位置由雜湊決定，與分數無關，不要讓高分的貓聚到某一區。
- **升級有慶祝，降級沒有懲罰表現。**
- **不做出席、警告、違規等負面標記。**
- `class-data.json` 進 `.gitignore`。學生姓名不進版控、不離開這台電腦。

---

## 9. 設定檔

`js/config.js` 要有完整中文註解，讓不寫程式的人也能改：

```js
const CONFIG = {
  // 畫面上方的標題
  TITLE: '',

  // 沒有指定時的預設滿分
  DEFAULT_MAX_HP: 100,

  // 加分按鈕的數值。想要多顆按鈕就填多個數字，例如 [1, 3, 5]
  AWARD_VALUES: [1],

  // 地圖大小（格數）。學生多就調大
  MAP_COLS: 30,
  MAP_ROWS: 20,

  // 換這個數字就會產生完全不同的地圖
  MAP_SEED: 20260901,

  // 是否在血條旁顯示數字。false 只顯示長度
  SHOW_HP_NUMBERS: true,

  // 等級門檻與稱號
  LEVELS: [ /* ... */ ],

  // 自動備份保留天數
  BACKUP_KEEP_DAYS: 30,
};
```

---

## 10. 不要做的事

以下每一項都是刻意排除的，不是遺漏。

- 不要加後端、資料庫、伺服器 API。啟動腳本只是靜態檔案伺服器。
- 不要加學生登入或任何帳號系統。
- 不要用 localStorage 或 sessionStorage。IndexedDB 只能用來存目錄 handle。
- 不要把成績存進任何瀏覽器儲存——檔案是唯一真相。
- 不要加音效或背景音樂。
- 不要加排行榜、名次、互相比較的排序。
- 不要做減分介面（復原除外）。
- 不要做任何死亡、淘汰、懲罰的視覺表現。
- 不要引入 React、Vue、Phaser、jQuery 或任何前端函式庫。
- 不要做檔案輪詢。
- 不要在程式裡寫死任何學生姓名。
- 不要用生成式 AI 產圖補素材。
- 不要為了支援 Firefox / Safari 而做降級版寫入方案。明確告知改用 Chrome 即可。

---

## 11. 分階段交付

每階段結束停下來，讓老師看過再繼續。

**階段一 — 骨架與地圖**
啟動腳本、靜態頁面、地圖產生與繪製。沒有貓、沒有資料。
驗收：雙擊啟動檔，瀏覽器自動開啟並顯示地圖；換 `MAP_SEED` 得到不同但同樣合理的地圖；同種子重整完全一致。

**階段二 — 貓與假資料**
讀 `class-data.example.json`（唯讀），貓生成、動畫、名字血條、四種血量狀態。
驗收：把假資料某人的 hp 從 90 改成 15，重整後那隻貓從站著變成蜷睡。

**階段三 — 檔案讀寫**
目錄授權、handle 記憶、讀取、寫入、去抖動、備份、schema 容錯、解析失敗的唯讀保護。此階段可以先用一顆臨時的 debug 按鈕觸發加分來驗證寫入。
驗收：加分後打開 `class-data.json` 看到新值與新 log；把檔案改成壞掉的 JSON，重載後看到錯誤橫幅且檔案未被覆寫。

**階段四 — 編輯模式與角色面板**
切換鈕、命中測試、面板滑入、大張貓動畫、+1、等級與升級表現、面板內紀錄列表。
驗收：關閉編輯模式時點貓無反應；開啟後加分，血條緩動、跨門檻播升級動畫、檔案同步更新。

**階段五 — 復原與收尾**
復原堆疊、快捷鍵、提示條、取消復原、面板內單筆復原。README（中文，寫給老師）、CREDITS、頁尾標示、`.gitignore`。
驗收：誤加分後 `Ctrl+Z` 正確回退且 log 留下兩筆紀錄；關掉網頁重開，昨天的紀錄仍可復原。

---

## 12. 還沒定案的

動工前跟老師確認：

1. **班級人數** — 影響地圖預設尺寸與標籤間距。先假設 35 人。
2. **滿分是多少、如何對應學期成績** — 目前 `DEFAULT_MAX_HP` 預設 100，但一學期實際能累積幾分沒定。若一節課加十幾分，一學期會遠超過 100，血條會長期滿格而失去意義。建議老師先估算「一學期大約會發出多少分」再回頭定滿分。
3. **等級門檻與稱號** — config 裡先放合理的預設值，之後由老師自己改。
