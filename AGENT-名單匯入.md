# 班級像素小鎮：名單匯入與資料維護指南 (給 AI 代理的交接文件)

你好，未來的 AI 代理。這是一份關於「班級像素小鎮」專案的交接文件，請你在處理任何名單匯入或資料修改前，務必仔細閱讀。

## 專案簡介與隱私說明
這是一個為單一教師設計的單機版班級像素小鎮網頁應用程式，以純靜態前端運行並透過檔案系統存取 API 讀寫本地資料。學生名稱屬於高度敏感的個人資料（PII），絕對不可外洩。因此，所有處理真實學生資料的操作必須僅限於本地執行，絕不能將真實姓名上傳或貼上至任何外部服務或大語言模型之中。

## 資料結構定義 (Schema)
系統唯一的真實資料來源為 `data/<CLASS_ID>.json`，請嚴格遵守以下結構與語意：
- `id`: 字串。程式自動生成的永久唯一識別碼（例如：`s001`, `s6a3b2c1`）。它會作為雜湊種子來決定畫面上貓咪的位置與外觀。**絕對不可重新指派或修改**，否則該學生的貓咪會在畫面上瞬間移動並改變顏色/外觀。
- `seat`: 數字。學生的座號（1-based）。在還沒有匯入真實姓名時，畫面上會只顯示這個座號；匯入後則會連同姓名一併顯示（例如「1. 王大明」）。
- `name`: 字串。學生的姓名。
- `xp`: 數字。學生目前累積的總分。等級是從分數推導而來，不會存在檔案中。
- `note`: 字串。教師填寫的備註。
- `log`: 陣列。所有加扣分與操作的**唯讀附加日誌 (append-only)**。每個日誌包含 `id` (日誌ID), `ts` (ISO 8601 UTC+8 時間戳記), `student_id` (關聯的學生ID), `delta` (分數變化), `xp_after` (變化後分數), `reason` (原因), `undone` (是否已撤銷)。

## 名單匯入標準作業流程 (SOP)
請依照以下步驟匯入或更新名單：
1. **準備名單**：將學生名單存入 `tools/names.txt`（UTF-8 編碼）。每行一個學生，可以只寫名字（依行號分配座號），或使用 `座號,姓名` 的格式。
2. **測試執行 (Dry-run)**：執行 `python tools/import_roster.py --dry-run`。
3. **確認結果**：仔細檢查終端機輸出的「預計新增」、「預計更新」與「警告保留」數量是否符合教師的預期。
4. **正式寫入**：確認無誤後，執行 `python tools/import_roster.py`。
5. **瀏覽器驗證**：請教師重新整理網頁，確認學生的貓咪數量與名稱是否正確顯示。

## 貓咪數量的控制邏輯
畫面上的貓咪數量是**完全跟隨資料檔中的學生人數**。`js/config.js` 中的 `CONFIG.PLACEHOLDER_STUDENT_COUNT` 只是在「還沒有真實名單」時產生佔位資料用的。一旦有了真實的 JSON 資料檔，要改變貓咪數量就只能透過匯入名單或增減資料檔裡的學生資料。

## 學期中名單異動處理方式
當學期中遇到轉學生加入、學生轉出或姓名更正時：
- **正確做法**：直接修改 `tools/names.txt`，並再次執行 `python tools/import_roster.py`。
- **機制說明**：腳本會依據「座號」進行安全合併。新的座號會被加入並獲得新的 `id`；舊座號若改名只會更新 `name` 且保留其 `id` 與 `xp`；若有座號從名單中消失，腳本會印出警告並**保留該學生資料**，絕不會默默刪除，以防手誤造成學生成績遺失。

## 絕對禁止事項 (Do NOT do this)
- **不可修改日誌**：絕不可編輯、刪除或重排 `log` 陣列中的項目。
- **不可重新編號 ID**：絕不可為了讓 `id` 連續而重新編排已存在的 `id`，這會破壞學生的貓咪外觀。
- **不可依成績排序**：匯出成績 (`export_grades.py`) 必須永遠依據「座號」排序，本系統不提供學生成績比較或排名功能。
- **不可將資料加入版本控制**：絕不可將 `data/*.json` 或是真實的 `tools/names.txt` 提交 (commit) 到 Git。
- **不可外洩資料**：絕不可將真實學生姓名貼入任何外部服務、翻譯工具或 AI 對話中。

## 匯入範例
**輸入檔案 (`tools/names.txt`)**:
```
王大明
2,李小華
```

**結果產生的 JSON (`data/class-data.json`)**:
```json
{
  "version": 2,
  "class_id": "class-data",
  "updated_at": "2026-09-01T10:23:45+08:00",
  "students": [
    {
      "id": "s8f7e6d5",
      "seat": 1,
      "name": "王大明",
      "xp": 0,
      "note": ""
    },
    {
      "id": "s1a2b3c4",
      "seat": 2,
      "name": "李小華",
      "xp": 0,
      "note": ""
    }
  ],
  "log": []
}
```
